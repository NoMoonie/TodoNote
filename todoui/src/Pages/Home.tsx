import React, { FC } from "react";
import IPage from "../Interfaces/page";

const Home: FC<IPage> = (props) => {
    return (
        <div>
            <h1>home</h1>
        </div>
    );
};

export default Home;
